mixin breadcrumbs(path, separator = '/')
  ul.breadcrumbs
    each item, idx in path
      li.breadcrumbs__item
        if idx === path.length - 1
          span= item
        else
          a(href='#')= `${item} ${separator}`